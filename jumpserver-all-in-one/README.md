# example

> ℹ️ 说明：本目录基于 JumpServer v4 的 `jms_all` 单镜像（all-in-one），自带 MySQL 8.4 与 Redis 8.0，无需外部依赖，开箱即用。

## Usage

```bash
cp .env.example .env # 如有
docker-compose up -d
```

访问： [http://127.0.0.1:1080](http://127.0.0.1:1080)
初始账号： admin
初始密码： ChangeMe
